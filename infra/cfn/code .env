APP_ENV=local
API_PORT=8000

# For Day-1 local scanning (your own AWS creds via aws configure)
AWS_REGION=us-east-1

# Local DB (simple)
DB_URL=sqlite:///./local.db

# Later (cloud)
DDB_ACCOUNTS_TABLE=accounts
DDB_SCANS_TABLE=scans
DDB_FINDINGS_TABLE=findings
SQS_SCAN_QUEUE_URL=
JWT_SECRET=change_me

